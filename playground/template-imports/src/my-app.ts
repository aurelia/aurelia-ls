/**
 * Main component that uses template-level <import> to bring in a child component.
 * Tests that <import from="./counter"> properly resolves and renders.
 */
export class MyAppCustomElement {
  public title = "Template Imports Demo";
}
