{
  "name": "aurelia-ls",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.17 <23"
  },
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "node scripts/clean.mjs",
    "test": "pnpm -w build && node --test",
    "test:spec": "pnpm -w build && node --test --test-reporter=spec",
    "test:domain": "pnpm -w build && node --test --test-reporter=spec packages/domain/test/**/*.test.mjs",
    "test:lower": "pnpm -F @aurelia-ls/domain build && node --test --test-reporter=spec packages/domain/test/lower/lower.test.mjs",
    "test:lower:cov": "pnpm -F @aurelia-ls/domain build && c8 --all --include \"packages/domain/out/compiler/phases/10-lower/*.js\" --exclude \"**/*.test.*\" --exclude \"packages/**/test/**\" -r text -r html -r lcov -o coverage/lower node --test --test-reporter=spec packages/domain/test/lower/lower.test.mjs",
    "test:resolve": "pnpm -w build && node --test --test-reporter=spec packages/domain/test/resolve/*.test.mjs",
    "test:resolve:dev": "pnpm -w build && node --test --inspect-brk --test-name-pattern=\"R-025\" --watch --test-reporter=spec packages/domain/test/resolve/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "c8": "^10.1.3",
    "typescript": "^5.5.4"
  }
}
