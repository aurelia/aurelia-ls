# Semantic compiler gallery host

This existing directory anchors module resolution for the AOT assurance gallery. The semantic oracle supplies its
TypeScript app source through semantic-runtime's project-input authority, so cases remain one canonical in-memory
corpus rather than checked-in generated fixtures.
