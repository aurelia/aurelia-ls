/** Temporal posture of one final compiler occurrence membership at context-structure bind time. */
export const enum TemplateCompilerOccurrenceMembershipArrivalPosture {
  Initial = 'initial',
  AdoptedInput = 'adopted-input',
  IncomingTransfer = 'incoming-transfer',
}
