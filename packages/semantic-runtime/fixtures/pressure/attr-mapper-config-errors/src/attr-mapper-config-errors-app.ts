import { customElement } from '@aurelia/runtime-html';
import template from './attr-mapper-config-errors-app.html';

@customElement({ name: 'attr-mapper-config-errors-app', template })
export class AttrMapperConfigErrorsApp {
  value = '';
}
