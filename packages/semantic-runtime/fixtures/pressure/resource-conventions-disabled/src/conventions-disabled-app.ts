import { customElement } from 'aurelia';
import template from './conventions-disabled-app.html';

@customElement({
  name: 'conventions-disabled-app',
  template,
})
export class ConventionsDisabledApp {}
