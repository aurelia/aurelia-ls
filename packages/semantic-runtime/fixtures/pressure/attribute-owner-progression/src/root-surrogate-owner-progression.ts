import { customElement } from '@aurelia/runtime-html';
import template from './root-surrogate-owner-progression.html';

@customElement({
  name: 'root-surrogate-owner-progression',
  template,
})
export class RootSurrogateOwnerProgression {
  message = 'message';
}
