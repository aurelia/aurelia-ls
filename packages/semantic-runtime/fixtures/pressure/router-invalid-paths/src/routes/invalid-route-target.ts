import { customElement } from '@aurelia/runtime-html';
import template from './invalid-route-target.html';

@customElement({ name: 'invalid-route-target', template })
export class InvalidRouteTarget {}
