import { customElement } from '@aurelia/runtime-html';
import template from './closed-open-alias-app.html';

@customElement({
  name: 'closed-open-alias-app',
  template,
})
export class ClosedOpenAliasApp {}
