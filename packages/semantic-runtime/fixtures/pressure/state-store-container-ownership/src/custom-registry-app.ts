import { customElement } from '@aurelia/runtime-html';
import template from './custom-registry-app.html';

@customElement({
  name: 'custom-registry-app',
  template,
})
export class CustomRegistryApp {}
