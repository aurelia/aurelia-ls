import { customElement } from '@aurelia/runtime-html';
import template from './runtime-html-switch-errors-app.html';

@customElement({ name: 'runtime-html-switch-errors-app', template })
export class RuntimeHtmlSwitchErrorsApp {
  mode = 'ready';
}
