import { customElement } from '@aurelia/runtime-html';
import template from './runtime-html-if-else-errors-app.html';

@customElement({ name: 'runtime-html-if-else-errors-app', template })
export class RuntimeHtmlIfElseErrorsApp {
  ready = true;
}
