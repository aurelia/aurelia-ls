# Source and build artifacts
src/**
out/**
test/**

# Exclude root node_modules (TypeScript is copied to dist/node_modules by esbuild.mjs)
# Leading / anchors to root — without it, dist/node_modules/ is also excluded
/node_modules/**

# Config files
tsconfig.json
tsconfig.tsbuildinfo
esbuild.mjs
.eslintrc*

# Development files
**/*.map

# Keep dist, images, package.json, README, CHANGELOG, LICENSE
