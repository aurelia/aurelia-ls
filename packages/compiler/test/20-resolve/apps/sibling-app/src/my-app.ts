/**
 * Root application component.
 *
 * Uses sibling-file convention: my-app.ts + my-app.html (no import needed).
 * This differs from convention-app which uses template-import convention.
 */
export class MyApp {
  message = "Hello from sibling-app!";
}
