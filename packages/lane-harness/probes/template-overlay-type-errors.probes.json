{
  "fixture": "packages/semantic-runtime/fixtures/pressure/template-overlay-type-errors",
  "lanes": {
    "diagnostics": [
      {
        "id": "overlay-type-errors-template",
        "file": "src/template-overlay-type-errors-app.html",
        "verdict": "correct",
        "reason": "The full document-diagnostic pull returns the semantic missing-member rows plus the public non-duplicated overlay TypeScript rows on authored template spans, with TS18046 grouped with the semantic weak-owner member evidence.",
        "note": "NEW 2026-07-08. Diagnostics lane canary for public template overlay TypeScript diagnostics: argument mismatch, arity mismatch, nullish access, unknown repeat local, and duplicate missing-member suppression. VERDICT 2026-07-08: correct. Six editor diagnostics are expected here: two semantic missing-member rows for known typed missing properties, TS18046 for the unknown repeat local with the member row as related/contextual evidence, TS18047 for strict nullish owner access, TS2345 for the wrong call argument, and TS2554 for the extra call argument. This follows the framework/tooling split: Aurelia non-strict ordinary property reads can produce undefined at runtime, so semantic missing-member rows remain authoring-policy warnings; overlay TypeScript rows carry blocking checker evidence when TypeScript proves a type/call/nullish mismatch. Presentation caveat, not a wrong verdict: the LSP code is the generic Aurelia diagnostic kind while TS#### lives in message/data, and missing-member wording could be more human/owner-type-specific."
      }
    ]
  }
}
